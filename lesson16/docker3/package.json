{
  "name": "sCalc",
  "version": "1.0.0",
  "description": "A simple calculator application",
  "main": "index.js",
  "scripts": {
    "build": "echo 'Building the project' && mkdir -p build && find . -not -path './build*' -not -path './node_modules*' -exec cp -r --parents {} build/ \\;"
  },
  "author": "Your Name",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {}
}
